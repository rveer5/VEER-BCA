x <- matrix(c(1, 2, 3, 4, 5, 6), nrow = 3, ncol = 2,
  byrow = TRUE
)
x

diag(1, nrow = 5)

#dimnames= list(c('r1', 'r2', r3),
#c('c1', 'c2')) is used to give names to matrix
# > rownames(B) <- c('r1','r2','r3') #Specify row name
# > colnames(B) <- c('c1','c2') #Specify col name